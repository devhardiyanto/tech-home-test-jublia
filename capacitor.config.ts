import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devhardiyanto.pokedex',
  appName: 'Pokédex',
  // Angular's application builder emits the web app under browser/.
  webDir: 'dist/tech-home-test/browser',
};

export default config;
