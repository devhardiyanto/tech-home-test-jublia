/* Production config. Values are baked in at build time — Capacitor native
   builds have no runtime environment to read from. */
export const environment = {
  production: true,
  pokeApiBaseUrl: 'https://pokeapi.co/api/v2',
  pageSize: 24,
};
